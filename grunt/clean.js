'use strict';

module.exports = {

  dist: [
    '<%= paths.dist %>/css',
    '<%= paths.dist %>/fonts',
    '<%= paths.dist %>/images',
    '<%= paths.dist %>/js',
    '<%= paths.dist %>/vendor'
  ],
  temp: '<%= paths.temp %>'

};
